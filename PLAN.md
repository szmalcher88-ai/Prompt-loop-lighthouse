# Plan zadań pętli — milestone 1: miasteczko nad morzem z latarnią

<!-- Format: jedna linia = jedno zadanie = jeden commit.
     - [ ] otwarte, [x] zrobione (oznacza pętla), [!] eskalowane.
     Ten plik należy do pętli. Agent ma zakaz go dotykać (guard automatyczny). -->

## Milestone 0 — latarnia (zamknięty)

- [x] Utwórz lighthouse.py (czysty Python, stdlib-only), który po `python lighthouse.py` zapisuje out/lighthouse.obj — model 3D latarni morskiej w układzie Y-up z nazwanymi grupami `o`: base (skalisty cokół), tower (cylindryczna wieża zwężająca się ku górze, min. 24 segmenty obwodu), gallery (galeryjka z balustradą pod laterną), lantern (przeszklona laterna), roof (stożkowy dach jako najwyższy punkt modelu), door (drzwi w dolnej części wieży); PRZED pisaniem przeczytaj scripts/check_lighthouse.py i spełnij wszystkie jego asercje geometryczne.
- [x] Rozszerz lighthouse.py o materiały: generuj dodatkowo out/lighthouse.mtl z co najmniej 3 materiałami (newmtl + kolory Kd, np. biały i czerwony pas, szkło laterny, ciemny dach), w OBJ dodaj `mtllib lighthouse.mtl` oraz `usemtl`, w tym co najmniej 2 naprzemienne materiały pasów na grupie tower — zgodnie z sekcją materiałów w scripts/check_lighthouse.py.

## Milestone 1 — miasteczko

- [x] Utwórz parts/terrain.py zgodnie z kontraktem parts/README.md: funkcja build(**params) zwraca pas wybrzeża — siatka terenu o zakresie co najmniej 40x40 m w XZ, w większości płaska, z łagodnym wzniesieniem (>= 1 m różnicy wysokości) pod latarnię od strony morza, wysokości w zakresie [-2, 12]; PRZED pisaniem przeczytaj funkcję check_terrain w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (determinizm, budżet "terrain" z budgets.json).
- [x] Utwórz parts/water.py zgodnie z kontraktem parts/README.md: build(**params) zwraca taflę morza — wszystkie wierzchołki dokładnie na y=0, zakres co najmniej 20x20 m w XZ, rozmiarem przylegająca do terenu z parts/terrain.py; PRZED pisaniem przeczytaj funkcję check_water w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (budżet "water").
- [x] Refaktor latarni do kontraktu części: utwórz parts/lighthouse.py z build(**params) zwracającym grupy latarni (w tym grupę o nazwie zawierającej "tower", zwężającą się ku górze, wysokość całości 10-200 m — patrz check_lighthouse w scripts/check_parts.py), a lighthouse.py w korzeniu zamień na cienki wrapper, który używa parts/lighthouse.py i zapisuje out/lighthouse.obj + out/lighthouse.mtl DOKŁADNIE tak, by scripts/check_lighthouse.py pozostał zielony BEZ żadnych modyfikacji (te same nazwy grup base/tower/gallery/lantern/roof/door i materiały pasów).
- [x] Utwórz parts/house.py zgodnie z kontraktem parts/README.md: build(**params) zwraca parametryczny dom (parametry: wymiary, kolor ścian, seed) o wysokości 2-15 m, z dwuspadowym dachem na materiale o nazwie zawierającej "roof" (centroid dachu powyżej centroidu ścian) i drzwiami na materiale zawierającym "door" sięgającymi podłoża domu; PRZED pisaniem przeczytaj funkcję check_house w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (budżet "house").
- [x] Utwórz parts/pier.py zgodnie z kontraktem parts/README.md: build(**params) zwraca drewniany pomost na palach wchodzący w morze — większość wierzchołków nad wodą y=0 (deski pomostu), pale sięgające poniżej y=-0.2; PRZED pisaniem przeczytaj funkcję check_pier w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (budżet "pier").
- [x] Utwórz scene.py — assembler sceny: scala części z parts/ (teren + woda + latarnia na wzniesieniu + 5-7 domów o zróżnicowanych parametrach wzdłuż wybrzeża + pomost wchodzący w morze), przesuwa indeksy, nadaje grupom prefiksy nazw (terrain*, water*, lighthouse*, house_1..house_N, pier*), zapisuje out/town.obj + out/town.mtl (mtllib/usemtl, >= 5 materiałów); wymagania rozmieszczenia przeczytaj w scripts/check_scene.py (latarnia najwyższa, domy posadowione na terenie >= 0 z tolerancją 0.6 m, rozłączne AABB w XZ, budżety sceny) — po tym zadaniu PEŁNY łańcuch verify musi być zielony, w tym scripts/render_test.py.

## Milestone 1 — runda poprawkowa (feedback bramy; wzorzec wersjonowanego kontraktu — parts/README.md)

- [x] Podnieś parts/terrain.py do CONTRACT_VERSION = 2 i spełnij sekcję check_terrain_v2 w scripts/check_parts.py (pas wybrzeża >= 80x60 m, >= 60% wierzchołków na lądzie, pas plaży 'sand*'/'beach*' >= 20 ścianek w wysokościach [-0.1, 1.2], łukowa linia brzegowa z odchyleniem >= 3 m od cięciwy, klif >= 2 m nad medianę poziomu lądu) ORAZ zaktualizuj scene.py tak, by domy/latarnia/pomost pozostały poprawnie posadowione na nowym terenie (check_scene v1 musi pozostać zielony; SCENE_VERSION zostaje 1).
- [x] Podnieś parts/pier.py do CONTRACT_VERSION = 2 i spełnij sekcję check_pier_v2 w scripts/check_parts.py (oś długa >= 12 m, >= 16 kolumn pali poniżej y=-0.2, >= 30 osobnych klepek 'plank*'/'deck*', balustrada 'rail*' >= 8 ścianek z poręczą >= 0.7 m nad deskami, >= 2 polery 'bollard*') ORAZ ustaw w scene.py pomost tak, by stykał się z plażą i wchodził w morze (check_scene v1 zielony).
- [x] Podnieś parts/house.py do CONTRACT_VERSION = 2 i spełnij sekcję check_house_v2 w scripts/check_parts.py (>= 2 okna 'glass*'/'window*', ramy/framuga 'frame*' >= 4 ścianki, komin 'chimney*' wystający >= 0.2 m ponad kalenicę, fundament 'foundation*'/'plinth*' sięgający podstawy) ORAZ zróżnicuj w scene.py instancje domów (wymiary/kolory per dom — przygotowanie pod asercję v2 "domy parami różne").
- [x] Utwórz parts/boat.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_boat w scripts/check_parts.py (dno zanurzone min-Y < -0.05, burty max-Y > 0.3, >= 2 ławki 'seat*'/'bench*' w różnych miejscach, budżet "boat") ORAZ dodaj w scene.py 2 instancje: boat_1 zacumowana < 6 m od środka pomostu, boat_2 na otwartej wodzie > 10 m od pomostu.
- [x] Utwórz parts/tree.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_tree w scripts/check_parts.py (wysokość 3-8 m, pień 'trunk*'/'bark*', korona 'leaves*'/'crown*' powyżej pnia, zwężająca się ku górze, budżet "tree") ORAZ dodaj w scene.py zagajnik >= 6 sosen (grupy tree_1..tree_N) o zróżnicowanych wysokościach za miasteczkiem, posadzonych na terenie.
- [x] Utwórz parts/rocks.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_rocks w scripts/check_parts.py (bryły zamknięte — wszystkie wierzchołki użyte w ściankach, niezerowe wymiary w 3 osiach, >= 8 wierzchołków per głaz, deterministycznie z seeda, budżet "rocks") ORAZ rozmieść w scene.py >= 10 głazów (grupy rock_1..rock_N) wzdłuż linii brzegowej i klifu, posadowionych na terenie.
- [x] Utwórz parts/path.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_path w scripts/check_parts.py (długość >= 10 m, różnica wysokości <= 4 m, budżet "path") ORAZ poprowadź w scene.py ścieżkę (grupa path*) od rejonu pomostu przez miasteczko do rejonu latarni, przylegającą do terenu (każdy wierzchołek <= 0.5 m od wysokości najbliższego wierzchołka terenu — patrz sekcja ścieżki w check_scene v2).
- [x] Podnieś scene.py do SCENE_VERSION = 2: finalny layout pasa wybrzeża spełniający WSZYSTKIE asercje v2 w scripts/check_scene.py (>= 2 łodzie przy pomoście i na wodzie, >= 6 drzew, >= 10 głazów, ścieżka pomost-latarnia, domy parami różne, pomost styka się z lądem i sięga >= 8 m w morze) oraz przekrojowy deterministyczny jitter kolorów per ścianka (parts/README.md) tak, by scripts/render_test.py przeszedł z progami v2 (>= 40 kolorów, wypełnienie kadru panoramy 25-65%) — po tym zadaniu PEŁNY łańcuch verify zielony.
