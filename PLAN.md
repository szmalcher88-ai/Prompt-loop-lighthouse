# Plan zadań pętli — milestone 1: miasteczko nad morzem z latarnią

<!-- Format: jedna linia = jedno zadanie = jeden commit.
     - [ ] otwarte, [x] zrobione (oznacza pętla), [!] eskalowane.
     Ten plik należy do pętli. Agent ma zakaz go dotykać (guard automatyczny). -->

## Milestone 0 — latarnia (zamknięty)

- [x] Utwórz lighthouse.py (czysty Python, stdlib-only), który po `python lighthouse.py` zapisuje out/lighthouse.obj — model 3D latarni morskiej w układzie Y-up z nazwanymi grupami `o`: base (skalisty cokół), tower (cylindryczna wieża zwężająca się ku górze, min. 24 segmenty obwodu), gallery (galeryjka z balustradą pod laterną), lantern (przeszklona laterna), roof (stożkowy dach jako najwyższy punkt modelu), door (drzwi w dolnej części wieży); PRZED pisaniem przeczytaj scripts/check_lighthouse.py i spełnij wszystkie jego asercje geometryczne.
- [x] Rozszerz lighthouse.py o materiały: generuj dodatkowo out/lighthouse.mtl z co najmniej 3 materiałami (newmtl + kolory Kd, np. biały i czerwony pas, szkło laterny, ciemny dach), w OBJ dodaj `mtllib lighthouse.mtl` oraz `usemtl`, w tym co najmniej 2 naprzemienne materiały pasów na grupie tower — zgodnie z sekcją materiałów w scripts/check_lighthouse.py.

## Milestone 1 — miasteczko

- [x] Utwórz parts/terrain.py zgodnie z kontraktem parts/README.md: funkcja build(**params) zwraca pas wybrzeża — siatka terenu o zakresie co najmniej 40x40 m w XZ, w większości płaska, z łagodnym wzniesieniem (>= 1 m różnicy wysokości) pod latarnię od strony morza, wysokości w zakresie [-2, 12]; PRZED pisaniem przeczytaj funkcję check_terrain w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (determinizm, budżet "terrain" z budgets.json).
- [x] Utwórz parts/water.py zgodnie z kontraktem parts/README.md: build(**params) zwraca taflę morza — wszystkie wierzchołki dokładnie na y=0, zakres co najmniej 20x20 m w XZ, rozmiarem przylegająca do terenu z parts/terrain.py; PRZED pisaniem przeczytaj funkcję check_water w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (budżet "water").
- [x] Refaktor latarni do kontraktu części: utwórz parts/lighthouse.py z build(**params) zwracającym grupy latarni (w tym grupę o nazwie zawierającej "tower", zwężającą się ku górze, wysokość całości 10-200 m — patrz check_lighthouse w scripts/check_parts.py), a lighthouse.py w korzeniu zamień na cienki wrapper, który używa parts/lighthouse.py i zapisuje out/lighthouse.obj + out/lighthouse.mtl DOKŁADNIE tak, by scripts/check_lighthouse.py pozostał zielony BEZ żadnych modyfikacji (te same nazwy grup base/tower/gallery/lantern/roof/door i materiały pasów).
- [x] Utwórz parts/house.py zgodnie z kontraktem parts/README.md: build(**params) zwraca parametryczny dom (parametry: wymiary, kolor ścian, seed) o wysokości 2-15 m, z dwuspadowym dachem na materiale o nazwie zawierającej "roof" (centroid dachu powyżej centroidu ścian) i drzwiami na materiale zawierającym "door" sięgającymi podłoża domu; PRZED pisaniem przeczytaj funkcję check_house w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (budżet "house").
- [x] Utwórz parts/pier.py zgodnie z kontraktem parts/README.md: build(**params) zwraca drewniany pomost na palach wchodzący w morze — większość wierzchołków nad wodą y=0 (deski pomostu), pale sięgające poniżej y=-0.2; PRZED pisaniem przeczytaj funkcję check_pier w scripts/check_parts.py i spełnij jej asercje oraz asercje wspólne (budżet "pier").
- [x] Utwórz scene.py — assembler sceny: scala części z parts/ (teren + woda + latarnia na wzniesieniu + 5-7 domów o zróżnicowanych parametrach wzdłuż wybrzeża + pomost wchodzący w morze), przesuwa indeksy, nadaje grupom prefiksy nazw (terrain*, water*, lighthouse*, house_1..house_N, pier*), zapisuje out/town.obj + out/town.mtl (mtllib/usemtl, >= 5 materiałów); wymagania rozmieszczenia przeczytaj w scripts/check_scene.py (latarnia najwyższa, domy posadowione na terenie >= 0 z tolerancją 0.6 m, rozłączne AABB w XZ, budżety sceny) — po tym zadaniu PEŁNY łańcuch verify musi być zielony, w tym scripts/render_test.py.

## Milestone 1 — runda poprawkowa (feedback bramy; wzorzec wersjonowanego kontraktu — parts/README.md)

- [x] Podnieś parts/terrain.py do CONTRACT_VERSION = 2 i spełnij sekcję check_terrain_v2 w scripts/check_parts.py (pas wybrzeża >= 80x60 m, >= 60% wierzchołków na lądzie, pas plaży 'sand*'/'beach*' >= 20 ścianek w wysokościach [-0.1, 1.2], łukowa linia brzegowa z odchyleniem >= 3 m od cięciwy, klif >= 2 m nad medianę poziomu lądu) ORAZ zaktualizuj scene.py tak, by domy/latarnia/pomost pozostały poprawnie posadowione na nowym terenie (check_scene v1 musi pozostać zielony; SCENE_VERSION zostaje 1).
- [x] Podnieś parts/pier.py do CONTRACT_VERSION = 2 i spełnij sekcję check_pier_v2 w scripts/check_parts.py (oś długa >= 12 m, >= 16 kolumn pali poniżej y=-0.2, >= 30 osobnych klepek 'plank*'/'deck*', balustrada 'rail*' >= 8 ścianek z poręczą >= 0.7 m nad deskami, >= 2 polery 'bollard*') ORAZ ustaw w scene.py pomost tak, by stykał się z plażą i wchodził w morze (check_scene v1 zielony).
- [x] Podnieś parts/house.py do CONTRACT_VERSION = 2 i spełnij sekcję check_house_v2 w scripts/check_parts.py (>= 2 okna 'glass*'/'window*', ramy/framuga 'frame*' >= 4 ścianki, komin 'chimney*' wystający >= 0.2 m ponad kalenicę, fundament 'foundation*'/'plinth*' sięgający podstawy) ORAZ zróżnicuj w scene.py instancje domów (wymiary/kolory per dom — przygotowanie pod asercję v2 "domy parami różne").
- [x] Utwórz parts/boat.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_boat w scripts/check_parts.py (dno zanurzone min-Y < -0.05, burty max-Y > 0.3, >= 2 ławki 'seat*'/'bench*' w różnych miejscach, budżet "boat") ORAZ dodaj w scene.py 2 instancje: boat_1 zacumowana < 6 m od środka pomostu, boat_2 na otwartej wodzie > 10 m od pomostu.
- [x] Utwórz parts/tree.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_tree w scripts/check_parts.py (wysokość 3-8 m, pień 'trunk*'/'bark*', korona 'leaves*'/'crown*' powyżej pnia, zwężająca się ku górze, budżet "tree") ORAZ dodaj w scene.py zagajnik >= 6 sosen (grupy tree_1..tree_N) o zróżnicowanych wysokościach za miasteczkiem, posadzonych na terenie.
- [x] Utwórz parts/rocks.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_rocks w scripts/check_parts.py (bryły zamknięte — wszystkie wierzchołki użyte w ściankach, niezerowe wymiary w 3 osiach, >= 8 wierzchołków per głaz, deterministycznie z seeda, budżet "rocks") ORAZ rozmieść w scene.py >= 10 głazów (grupy rock_1..rock_N) wzdłuż linii brzegowej i klifu, posadowionych na terenie.
- [x] Utwórz parts/path.py (CONTRACT_VERSION = 2) zgodnie z kontraktem parts/README.md i sekcją check_path w scripts/check_parts.py (długość >= 10 m, różnica wysokości <= 4 m, budżet "path") ORAZ poprowadź w scene.py ścieżkę (grupa path*) od rejonu pomostu przez miasteczko do rejonu latarni, przylegającą do terenu (każdy wierzchołek <= 0.5 m od wysokości najbliższego wierzchołka terenu — patrz sekcja ścieżki w check_scene v2).
- [x] Podnieś scene.py do SCENE_VERSION = 2: finalny layout pasa wybrzeża spełniający WSZYSTKIE asercje v2 w scripts/check_scene.py (>= 2 łodzie przy pomoście i na wodzie, >= 6 drzew, >= 10 głazów, ścieżka pomost-latarnia, domy parami różne, pomost styka się z lądem i sięga >= 8 m w morze) oraz przekrojowy deterministyczny jitter kolorów per ścianka (parts/README.md) tak, by scripts/render_test.py przeszedł z progami v2 (>= 40 kolorów, wypełnienie kadru panoramy 25-65%) — po tym zadaniu PEŁNY łańcuch verify zielony.

## Milestone 2 „Forma” — wyspa wg referencji (ART_DIRECTION.md; kontrakt v3)

- [x] Podnieś parts/terrain.py do CONTRACT_VERSION = 3 i spełnij sekcję check_terrain_v3 w scripts/check_parts.py (WYSPA: cały obrys siatki pod wodą, udział lądu 20-75%, >= 3 poziomy tarasowe rozdzielone >= 1 m skupiające po >= 5% lądu, >= 8 miejsc klifowych z uskokiem >= 2.5 m, zatoczka z plażą 'sand*' >= 10 ścianek wciętą >= 2 m w obrys lądu od strony -Z, turnia pod kapliczkę jako lokalne wzniesienie rozdzielone przełęczą od masywu — wzorzec: scripts/fixtures/parts_good_v3/terrain.py) ORAZ przestaw layout w scene.py na nowy ląd tak, by WSZYSTKIE asercje v2 check_scene pozostały zielone; UWAGA na dwie asercje, które poprzednio zostały czerwone: (a) pomost ma WYCHODZIĆ z plaży zatoczki w stronę -Z POZA obrys lądu — koniec pomostu >= 8 m od najbliższego wierzchołka terenu o y > 0.05, początek <= 2 m od takiego wierzchołka; (b) przesuń łodzie: boat_1 na wodzie < 6 m od środka pomostu, boat_2 na otwartej wodzie > 10 m od niego. Wzorzec całego rozmieszczenia na wyspie (pozycje pomostu, łodzi, domów, ścieżki): scripts/fixtures/gen_scene_v3.py — przeczytaj go PRZED pisaniem; SCENE_VERSION zostaje 2.
- [x] Utwórz parts/wall.py (CONTRACT_VERSION = 3) zgodnie z sekcją check_wall w scripts/check_parts.py (>= 8 ścianek muru 'wall*'/'stone*' o wysokości >= 0.5 m, schody 'stair*'/'step*' o >= 6 poziomach stopni pokonujące >= 1 m) ORAZ dodaj w scene.py >= 2 segmenty muru (grupy wall_1, wall_2) wzdłuż krawędzi tarasów i >= 1 ciąg schodów (grupa stair_1) między poziomami.
- [x] Podnieś parts/house.py do CONTRACT_VERSION = 3: build(archetype=...) z trzema archetypami "timber"/"stone"/"two_story" wg sekcji check_house_v3 w scripts/check_parts.py (timber: >= 6 belek 'beam*'/'timber*'; stone: >= 6 ścianek 'stone*', wysokość <= 4.5 m, 1 pasmo okien; two_story: wysokość >= 5 m, >= 2 pasma okien rozdzielone > 1.2 m; każdy archetyp spełnia też v1+v2, geometrie parami różne — wzorzec: scripts/fixtures/parts_good_v3/house.py) ORAZ rozmieść w scene.py >= 9 domów na tarasach w grupach nazwanych house_<k>_<archetyp> (wszystkie 3 archetypy użyte, każdy dom z sąsiadem <= 12 m, AABB rozłączne, posadowione na terenie).
- [x] Utwórz parts/chapel.py i parts/bridge.py (CONTRACT_VERSION = 3) zgodnie z sekcjami check_chapel i check_bridge w scripts/check_parts.py (kapliczka: dach 'roof*' wieńczący bryłę + sygnaturka 'cross*'/'spire*' jako najwyższy punkt, plan <= 25 m2, wysokość <= 7 m; mostek: >= 2 liny 'rope*' rozstawione >= 0.4 m ze zwisem >= 0.2 m, >= 8 desek 'plank*', długość >= 4 m) ORAZ postaw w scene.py kapliczkę (grupa chapel_1) na turni (teren >= 3 m pod kapliczką, >= 6 m od domów) z mostkiem (grupa bridge_1) dochodzącym <= 5 m do turni i <= 6 m do zabudowy/muru.
- [x] Podnieś parts/pier.py do CONTRACT_VERSION = 3 (asercje v2 bez zmian) i utwórz parts/prop_barrel.py oraz parts/prop_crate.py (CONTRACT_VERSION = 3) zgodnie z sekcjami check_prop_barrel i check_prop_crate w scripts/check_parts.py (beczka: wysokość 0.6-1.3 m, przekrój obły, >= 24 wierzchołki, >= 2 obręcze 'hoop*' na różnych wysokościach; skrzynia: wymiary 0.4-1.3 m, >= 4 listwy 'slat*'/'trim*') ORAZ ustaw w scene.py >= 2 segmenty pomostów (grupy pier_1, pier_2 — styk z lądem i >= 8 m w morze) oraz >= 4 beczki (grupy barrel_1..) i >= 3 skrzynie (grupy crate_1..) na pomostach lub nabrzeżu (<= 5 m od pomostu lub <= 3 m od brzegu).
- [x] Utwórz parts/bush.py (CONTRACT_VERSION = 3) zgodnie z sekcją check_bush w scripts/check_parts.py (wysokość 0.5-1.5 m, zieleń 'bush*'/'leaves*', >= 12 wierzchołków, >= 3 poziomy wysokości — nieregularna bryła) ORAZ rozmieść w scene.py >= 8 krzewów (grupy bush_1..) między zabudową (<= 10 m od najbliższego domu) i skoryguj zagajnik sosen pod nowy ląd wyspy.
- [x] Zaktualizuj w scene.py łódki i głazy pod układ wyspy: >= 1 łódka na piasku plaży zatoczki (boat* z terenem > 0.05 pod centroidem; obok łodzi przy pomoście < 6 m i na otwartej wodzie > 10 m z asercji v2) oraz pierścień >= 8 głazów W WODZIE poza obrysem wyspy (rock* z terenem < -0.2 pod centroidem; łącznie >= 10 głazów) — sekcje zatoczki i pierścienia skał w check_scene v3 (na razie nieaktywne, SCENE_VERSION zostaje 2 — zadanie przygotowuje finał).
- [x] Podnieś scene.py do SCENE_VERSION = 3: finalny layout wyspy wg ART_DIRECTION.md spełniający WSZYSTKIE asercje v3 w scripts/check_scene.py (latarnia na najwyższym plateau, kapliczka na turni z mostkiem, >= 9 domów ciasno w 3 archetypach, mury+schody, 2 pomosty z rekwizytami, >= 8 krzewów, łódka na plaży, pierścień skał w wodzie) tak, by scripts/render_test.py przeszedł z progami v3 (wypełnienie kadru panoramy 30-70%) — po tym zadaniu PEŁNY łańcuch verify zielony.

## Milestone 2.5 „Światło na bramę" — korekty layoutu (kontrakt v4; render bramy = Blender)

- [x] Podnieś parts/terrain.py do CONTRACT_VERSION = 4: zachowaj wszystkie własności v3 (check_terrain_v3 ma pozostać zielony: wyspa, >= 3 tarasy, klify, zatoczka z plażą, turnia) ALE uczyń obrys wyspy ORGANICZNYM — promień brzegu modulowany kilkoma harmonicznymi, tak by linia brzegowa miała >= 8 lokalnych ekstremów i CoV promienia >= 0.06 (to sprawdza check_scene v4(b) na zmontowanej scenie; wzorzec funkcji promienia: scripts/fixtures/gen_scene_v4.py, r_organic) — ORAZ dostosuj scene.py do nowego terenu tak, by WSZYSTKIE asercje check_scene v3 pozostały zielone (SCENE_VERSION zostaje 3; pełny łańcuch matplotlib-verify zielony).
- [x] W scene.py rozmieść domy w >= 3 GRONACH zamiast pierścienia (kontrakt parts/house.py BEZ ZMIAN — to zmiana LAYOUTU w assemblerze): grona o różnej liczności (np. 4/3/2, łącznie >= 9), każdy dom z sąsiadem <= 7.5 m w obrębie grona (brak singletonów), grona rozmieszczone tak, by CoV promienia domów od ich wspólnego centroidu >= 0.15 (anty-pierścień) — patrz sekcja v4(a) w scripts/check_scene.py i wzorzec rozmieszczenia w scripts/fixtures/gen_scene_v4.py; SCENE_VERSION zostaje 3, pełny łańcuch matplotlib-verify zielony (asercje v2/v3 domów: parami różne, posadowione, rozłączne AABB — nadal zielone).
- [x] Podnieś scene.py do SCENE_VERSION = 4: ustaw kapliczkę i mostek po STRONIE kamery głównej (gate_cameras.main_spec) i w jej frustum — centroid kapliczki i mostka po tej samej stronie środka wyspy co kamera (sekcja v4(c) w scripts/check_scene.py) — tak, by przy SCENE_VERSION = 4 CAŁY łańcuch matplotlib-verify był zielony, w tym scripts/render_test.py z progiem v4 (wypełnienie kadru panoramy 30-70%). Wzorzec całej sceny v4: scripts/fixtures/gen_scene_v4.py (tryb good). Render Blendera (scripts/render_blender.py) jest bramą po biegu, NIE częścią verify_commands — nie uruchamiaj go.

## Milestone 2.6 „Styki" — relacje przestrzenne (kontrakt v5; korekta layoutu w assemblerze)

- [x] W scene.py skoryguj pozycję/wymiary schodów i muru (grupy stair*/wall*) tak, by spełniały sekcję v5(a) w scripts/check_scene.py: ŻADNA bryła stair*/wall* nie przenika AABB żadnego domu (przenikanie na 3 osiach > 0.3 m -> FAIL — kolizja dom-infrastruktura), a każdy ciąg schodów (stair*) styka się z dwoma poziomami które łączy (dno schodów ~ teren przy dolnym końcu, szczyt ~ teren przy górnym końcu, tol 0.7 m — wysokość schodów = przyrost terenu między końcami). Dozwolona drobna parametryzacja parts/wall.py (np. parametr height/steps) BEZ zmiany kontraktu części. Wzorzec poprawnego styku: scripts/fixtures/gen_scene_v5.py (sekcja schodów). SCENE_VERSION zostaje 4; pełny łańcuch matplotlib-verify zielony.
- [x] W scene.py skoryguj posadowienie łódek (grupy boat*) wg sekcji v5(b) w scripts/check_scene.py: łódka nad wodą (teren pod centroidem < 0.05) — dno kadłuba (min-Y) w przedziale [-0.30, 0.30] wokół tafli y=0 i NIE poniżej -0.40 (nie tonie), burty (max-Y) > 0.20 nad taflą; łódka nad lądem (teren > 0.05) — spoczywa na piasku (min-Y ~ teren, tol 0.4). Wzorzec: łódki w scripts/fixtures/gen_scene_v5.py (boat_1 pływająca, boat_3 plażowa). SCENE_VERSION zostaje 4; pełny łańcuch matplotlib-verify zielony.
- [x] Podnieś scene.py do SCENE_VERSION = 5: domknij styk mostka/ścieżki do kapliczki wg sekcji v5(c) w scripts/check_scene.py — oba końce konektora (bridge*/path*) stykają się z gruntem (najniższy wierzchołek przy każdym końcu <= 1.2 m nad terenem w tym miejscu — przyczółki nie wiszą), a koniec od strony turni dochodzi do kapliczki (odstęp AABB konektora od AABB chapel* <= 2.0 m — nie urywa się przed progiem). Mostek może śledzić teren per-wierzchołkowo (wzorzec: funkcja drape w scripts/fixtures/gen_scene_v5.py). Po tym zadaniu CAŁY łańcuch matplotlib-verify zielony z asercjami v5 (w tym render_test). Render Blendera to brama po biegu — nie uruchamiaj go.

## Milestone 3 — migracja: latarnia w bpy (nowy fundament; jedno zadanie)

- [x] Utwórz parts/lighthouse_bpy.py w nowym kontrakcie bpy (sekcja "Kontrakt bpy" w parts/README.md): funkcja build(seed=0) tworzy w bieżącej scenie bpy obiekty nazwane base/tower/gallery/lantern/roof/door, z modyfikatorami (bevel + subdivision) i materiałami proceduralnymi (kamień, naprzemienne pasy biel/czerwień na wieży, szkło laterny, metal). Część NIE eksportuje — eksport robi scripts/bpy_build.py. Forma jak obecna latarnia (pionowa kolejność części, drzwi w dolnej wieży, dach najwyżej, wieża zwężająca się ku górze). WZORZEC do naśladowania: scripts/fixtures/lighthouse_bpy_ref.py (przeczytaj go w całości — to działająca część bpy). Cel: scripts/check_lighthouse.py (uruchamiany bez argumentów: build przez blender + TEST DETERMINIZMU eksportu = dwa build+export identyczne + asercje formy/materiałów) kończy się kodem 0. Determinizm: zero losowości (seed zarezerwowany, wynik niezależny od seeda). Możesz uruchamiać `python scripts/check_lighthouse.py` lokalnie, by się sprawdzić.

## Milestone 4 FAZA B — bieg dowodowy: 7 builderów rdzenia na bpy od zera

<!-- Wariant (b): cele parts/<typ>_bpy.py i parts/scene_bpy.py startują PUSTE (nieobecne).
     Agent pisze każdy builder OD ZERA, mając gotowy, działający wzorzec _ref jako
     widoczną specyfikację. Wzorce (scripts/fixtures/*_ref.py), wszystkie checkery,
     fixtures, scene.py i części OBJ (parts/*.py) są CHRONIONE — to sygnał prawdy,
     którego pętla nie dotyka. Każde zadanie = jeden builder = jeden commit, bramkowany
     swoim checkerem (forma stdlib przez check_parts + geometria bpy przez dedykowany
     test). Kolejność = graf zależności: części rdzenia -> assembler na końcu. -->

- [x] Utwórz parts/terrain_bpy.py w kontrakcie bpy (sekcja "Kontrakt bpy" w parts/README.md): build(seed=0, **params) pod Blenderem tworzy obiekt(y) terenu w układzie Z-up (transform wierzchołka (x,y,z) Y-up -> (x,-z,y)), bez eksportu i bez modyfikatorów; w trybie headless (bpy niedostępne) zwraca grupy stdlib REUŻYWAJĄC geometrii parts/terrain.py. Interfejs wysokości MUSI być zachowany co do bajta: wysokość odczytana z eksportu bpy w danym XZ == parametryczna wysokość tarasów (parts/terrain.py.height) == terrain OBJ. WZORZEC do naśladowania w całości: scripts/fixtures/terrain_bpy_ref.py. Bramki (kod 0): scripts/test_terrain_bpy.py (dwa buildy przez Blender = determinizm + interfejs wysokości na WYEKSPORTOWANEJ geometrii + known-bad: płaski teren odrzucony) ORAZ scripts/check_parts.py (forma stdlib: struktura, determinizm, budżet). Zero losowości (seed zarezerwowany). Możesz uruchamiać `python scripts/test_terrain_bpy.py`.
- [x] Utwórz parts/water_bpy.py w kontrakcie bpy: build(seed=0, **params) tworzy taflę morza — wszystkie wierzchołki eksportu dokładnie na y=0 (po transformie Z-up), bez modyfikatorów; tryb headless zwraca grupy stdlib reużywając parts/water.py. WZORZEC: scripts/fixtures/water_bpy_ref.py (przeczytaj w całości). Bramki (kod 0): scripts/test_bpy_parts.py water (determinizm + tafla na y=0 na eksporcie + known-bad: tafla podniesiona odrzucona) ORAZ scripts/check_parts.py. Zero losowości. Możesz uruchamiać `python scripts/test_bpy_parts.py water`.
- [x] Utwórz parts/house_bpy.py w kontrakcie bpy z trzema archetypami: build(seed=0, archetype="timber"|"stone"|"two_story", **params) tworzy dom (mały bevel, materiały proceduralne ścian/dachu/okien), GEOMETRYCZNIE różny per archetyp; tryb headless zwraca grupy stdlib reużywając parts/house.py. WZORZEC: scripts/fixtures/house_bpy_ref.py (przeczytaj w całości). Bramki (kod 0): scripts/test_bpy_parts.py house (3 archetypy parami różne wymiarami/liczbą wierzchołków + determinizm per archetyp na eksporcie) ORAZ scripts/check_parts.py. Zero losowości. Możesz uruchamiać `python scripts/test_bpy_parts.py house`.
- [x] Utwórz parts/wall_bpy.py w kontrakcie bpy — mur + schody: build(seed=0, **params) tworzy segment muru (ścianki nazwane 'wall*', >= 8 ścianek) ORAZ ciąg schodów (nazwane 'stair*', >= 6 poziomów stopni pokonujących różnicę poziomu); mały bevel; tryb headless zwraca grupy stdlib reużywając parts/wall.py. WZORZEC: scripts/fixtures/wall_bpy_ref.py (przeczytaj w całości). Bramki (kod 0): scripts/test_bpy_parts.py wall (>= 8 ścianek muru + >= 6 poziomów stopni na eksporcie + known-bad: spłaszczenie odrzucone) ORAZ scripts/check_parts.py. Zero losowości. Możesz uruchamiać `python scripts/test_bpy_parts.py wall`.
- [ ] Utwórz parts/chapel_bpy.py w kontrakcie bpy: build(seed=0, **params) tworzy kapliczkę z dachem ('roof*') wieńczącym bryłę i sygnaturką/krzyżem ('cross*') jako NAJWYŻSZYM punktem modelu; mały bevel, materiały proceduralne; tryb headless zwraca grupy stdlib reużywając parts/chapel.py. WZORZEC: scripts/fixtures/chapel_bpy_ref.py (przeczytaj w całości). Bramki (kod 0): scripts/test_bpy_parts.py chapel (dach obecny + krzyż najwyższy na eksporcie + known-bad: ściana ponad krzyż odrzucona) ORAZ scripts/check_parts.py. Zero losowości. Możesz uruchamiać `python scripts/test_bpy_parts.py chapel`.
- [ ] Utwórz parts/lighthouse_bpy.py w kontrakcie bpy: build(seed=0) tworzy obiekty base/tower/gallery/lantern/roof/door z modyfikatorami (bevel + subsurf) i materiałami proceduralnymi (kamień, szkło laterny, metal); wieża zwęża się ku górze i ma NAPRZEMIENNE POZIOME PASY biel/czerwień jako realne pierścienie obwodu (>= 4 naprzemienne pasma na wysokości wieży — nie tylko czapeczki na końcach). WZORZEC do naśladowania w całości: scripts/fixtures/lighthouse_bpy_ref.py. Bramki (kod 0): scripts/check_lighthouse.py (dwa buildy przez Blender = determinizm eksportu + asercje formy/materiałów + asercja naprzemiennych pasów wieży) ORAZ scripts/check_parts.py. Determinizm: zero losowości (seed zarezerwowany). Możesz uruchamiać `python scripts/check_lighthouse.py`.
- [ ] Utwórz parts/scene_bpy.py — assembler HYBRYDOWEJ sceny (CEL FINAŁOWY, najtrudniejszy): (a) core_placements() zwraca listę rozmieszczeń rdzenia — 6 typów bpy (terrain/water/lighthouse + grona house_* + wall_*/stair_* + chapel_*) na pozycjach z layoutu scene.py v5, z transformem _loc(x,y,z)=(x,-z,y) i posadowieniem z interfejsu wysokości terenu; (b) tryb CLI `--drobiazgi` uruchamia scene.assemble() i zapisuje out/drobiazgi.obj zawierające WYŁĄCZNIE drobiazgi OBJ (pier/boat/bridge/bush/tree/rock/path/propsy — grupy rdzenia odfiltrowane). scripts/bpy_build.py (tryb scene) importuje ten plik, buduje rdzeń przez buildery bpy na pozycjach z core_placements() i scala z out/drobiazgi.obj w jeden out/town.obj. WZORZEC do naśladowania w całości: scripts/fixtures/scene_hybrid_ref.py. Bramka (kod 0): scripts/test_hybrid_scene.py — check_scene v5 PRZECHODZI na żywej hybrydzie, pełna scena deterministyczna (dwa buildy = identyczny town.obj), a 3 perturbacje styku bpy<->OBJ ROZBIJAJĄ asercje z właściwym komunikatem (łódka OBJ pod wodę bpy -> "tonie"; mostek OBJ z dala od kapliczki bpy -> "nie dochodzi do kapliczki"; dom bpy w lewitację -> "lewitacja"). Determinizm: zero losowości. Możesz uruchamiać `python parts/scene_bpy.py --drobiazgi` oraz `python scripts/test_hybrid_scene.py`.
